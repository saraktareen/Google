.links{
    text-align:right;
}

body {
    margin: 1;
}

/* LOGO */
section {
    display: flex;
    flex-direction: column;
    align-items: center;
    margin-top: 8vh; /* Space from the very top of the webpage */
}

@font-face {
    font-family: 'Product Sans';
    font-style: normal;
    src: url(https://fonts.gstatic.com/s/productsans/v5/HYvgU2fE2nRJvZ5JFAumwegdm0LZdjqr5-oayXSOefg.woff2) format('woff2');
}

h1{
    font-family: 'Product Sans';
    font-weight: 608;
    font-size: 4.5em;
    height: 72px; /* Height of the Google logo */
    transform: scale(1.2,1.2); /* Transformation of the 'Google' logo to be a bit bigger */
}

#e {
    transform: rotate(315deg); /* For the rotated letter 'e' in Google */
}

/* SEARCH BAR */

/* Text bar */
input[type=text], select {
    display: flex; 
    justify-content: space-between;
    width: 600px; /*Width of the textbox*/
    height: 38px; /* Height of the textbox */
    border: 1px solid gainsboro; /*Border of the textbox*/
    border-radius: 20px; /* Round edges of the textbox*/
}

/* When the cursor touches the textbar */
input[type="text"]:hover {
    box-shadow:0 0 10px 0 gainsboro;
}

/* Buttons below the textbar */
input[type="submit"]  {
    font-family: arial, sans-serif;
    text-align: center;
    background-color: #f8F9FA;
    color: #222222;
    border:none;
    border-radius: 3px;
    height: 36px; /*Height of the button*/
    padding: 0 16px; /*Size of the button*/
    margin: 11px 14px; /* Increases the space between the buttons, creating margin space*/
    font-size:14px;
}

/* When the cursor touches the buttons*/
input[type="submit"]:hover {
    cursor: pointer;
    border: solid 1px gainsboro;
    box-shadow: 0 0 2px 0 gainsboro
}

/* IMAGES IN SEARCHBAR */


/* FOOTER */

nav{
    display: flex;
    justify-content: space-between; /* Gives space between the left and right part of the footer */
}

ul > a {
    padding: 0 20px; /* Padding for all links in the footer */
}

footer {
    background-color: #F2F2F2; /* Colour of the footer */
    position: fixed;
    flex-wrap:wrap;
    bottom: 0;
    width: 100vw;
}

/* Top  part of the footer */
#footer-country {
    display: flex;
    color: #0000008A;
    font-size: 15px;
    font-family: arial, sans-serif;
    justify-self: start;
    padding-left: 20px;
    align-items: left;
  
}

/* Creates a vertical line in the footer */
.vertical-line {
    border-top: 1px solid #d3d3d3;
    height: 50px;
}

/* Left side of the footer */
#footer-left {
    display: flex;
    justify-self: start;
    font-size: 14px;
    font-family: arial, sans-serif;
    padding-left: 2px;
    align-items: center; 
}

/* All links in the left footer */
#footer-left > a{
    text-decoration:none; 
    color: #70757A;
}

#footer-left:hover {
    text-decoration:underline;
    cursor: pointer;
}

/* Right side of the footer */
#footer-right {
    display: flex;
    justify-content: flex-end;
    font-size: 14px;
    font-family: arial, sans-serif;
    margin-right: 15px;
    align-items:center;
}

/* All the links in the right side of the footer */
#footer-right > a{
    text-decoration:none; 
    color: #70757A;
}

#footer-right:hover {
    text-decoration:underline;
    cursor: pointer;
}

/* Responsiveness of the webpage */
@media(max-width: 630px) {
    #footer-nav {
        flex-direction: column;
    }

    #footer-left {
        justify-content: center;
    }

    #footer-right {
        justify-content: center;
    }

    .textbox {
        width: 95vw;
    }
}

@media (max-height: 522px){
    body{
        overflow-x: hidden;
    }

    section{
        margin-bottom: 20px;
    }
    footer{
        position:relative;
    }
}

@media (max-heigt:570px) and (max-width: 630px) {
    body{
        overflow-x: hidden;
    }

    section{
        margin-bottom:13px;
    }

    footer{
        position:relative;
    }
}